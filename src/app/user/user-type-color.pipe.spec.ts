import { UserTypeColorPipe } from './user-type-color.pipe';

describe('UserTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new UserTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
